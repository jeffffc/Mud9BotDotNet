name: Mud9Bot CI/CD Console

on:
  push:
    branches: [ main ]
  repository_dispatch:
    types: [trigger_build_bot, trigger_build_web, trigger_deploy_bot, trigger_deploy_web]

jobs:
  notify-push:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      bot: ${{ steps.filter.outputs.bot }}
      web: ${{ steps.filter.outputs.web }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            bot:
              - 'Mud9Bot/**'
              - 'Mud9Bot.Data/**'
            web:
              - 'Mud9Bot.Web/**'
              - 'Mud9Bot.Data/**'

      # 🚀 修正：使用 %2B 代替 +，防止被 curl 轉換成空格
      - name: Send Bot Notification
        if: steps.filter.outputs.bot == 'true'
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_LOG_GROUP_ID }}" \
          -d "parse_mode=HTML" \
          -d "text=🤖 <b>[Bot] New Changes Detected</b>%0AAuthor: <b>${{ github.actor }}</b>%0ASHA: <code>${{ github.sha }}</code>%0AMsg: <code>${{ github.event.head_commit.message }}</code>" \
          -d "reply_markup={\"inline_keyboard\":[[{\"text\":\"🔨 Build Bot\",\"callback_data\":\"GH_BUILD%2BBOT%2B${{ github.sha }}\"},{\"text\":\"❌ Cancel\",\"callback_data\":\"GH_CANCEL%2BBOT%2B${{ github.sha }}\"}]]}"

      # 🚀 修正：使用 %2B 代替 +
      - name: Send Web Notification
        if: steps.filter.outputs.web == 'true'
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_LOG_GROUP_ID }}" \
          -d "parse_mode=HTML" \
          -d "text=🌐 <b>[Web] New Changes Detected</b>%0AAuthor: <b>${{ github.actor }}</b>%0ASHA: <code>${{ github.sha }}</code>%0AMsg: <code>${{ github.event.head_commit.message }}</code>" \
          -d "reply_markup={\"inline_keyboard\":[[{\"text\":\"🔨 Build Web\",\"callback_data\":\"GH_BUILD%2BWEB%2B${{ github.sha }}\"},{\"text\":\"❌ Cancel\",\"callback_data\":\"GH_CANCEL%2BWEB%2B${{ github.sha }}\"}]]}"

  build-bot:
    if: github.event.action == 'trigger_build_bot'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.client_payload.sha }}
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Build Bot
        run: dotnet publish Mud9Bot/Mud9Bot.csproj -c Release -o ./publish_bot
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: bot-pkg-${{ github.event.client_payload.sha }}
          path: publish_bot/
      
      # 🚀 修正：使用 %2B 代替 +
      - name: Notify Bot Build Success
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_LOG_GROUP_ID }}" \
          -d "parse_mode=HTML" \
          -d "text=✅ <b>Bot Build Success!</b>%0ASHA: <code>${{ github.event.client_payload.sha }}</code>%0A%0A已完成編譯，準備好部署 Bot 嗎？" \
          -d "reply_markup={\"inline_keyboard\":[[{\"text\":\"🚀 Deploy Bot\",\"callback_data\":\"GH_DEPLOY%2BBOT%2B${{ github.event.client_payload.sha }}\"},{\"text\":\"❌ Cancel\",\"callback_data\":\"GH_CANCEL%2BBOT%2B${{ github.event.client_payload.sha }}\"}]]}"

  build-web:
    if: github.event.action == 'trigger_build_web'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.client_payload.sha }}
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Build Web
        run: dotnet publish Mud9Bot.Web/Mud9Bot.Web.csproj -c Release -o ./publish_web
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-pkg-${{ github.event.client_payload.sha }}
          path: publish_web/
      
      # 🚀 修正：使用 %2B 代替 +
      - name: Notify Web Build Success
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_LOG_GROUP_ID }}" \
          -d "parse_mode=HTML" \
          -d "text=✅ <b>Web Build Success!</b>%0ASHA: <code>${{ github.event.client_payload.sha }}</code>%0A%0A已完成編譯，準備好部署 Web 嗎？" \
          -d "reply_markup={\"inline_keyboard\":[[{\"text\":\"🚀 Deploy Web\",\"callback_data\":\"GH_DEPLOY%2BWEB%2B${{ github.event.client_payload.sha }}\"},{\"text\":\"❌ Cancel\",\"callback_data\":\"GH_CANCEL%2BWEB%2B${{ github.event.client_payload.sha }}\"}]]}"

  deploy-bot:
    if: github.event.action == 'trigger_deploy_bot'
    runs-on: ubuntu-latest
    steps:
      # 🚀 修正：直接在部署階段重新 Checkout 與編譯，避開跨執行緒的 Artifact 限制
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.client_payload.sha }}
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Build Bot
        run: dotnet publish Mud9Bot/Mud9Bot.csproj -c Release -o ./publish_bot

      - name: Deploy Bot to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "publish_bot/"
          target: "${{ secrets.BOT_DEPLOY_PATH }}/releases/${{ github.event.client_payload.sha }}"
          strip_components: 1
      - name: Swap Bot & Restart
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            B_PATH="${{ secrets.BOT_DEPLOY_PATH }}"
            SHA="${{ github.event.client_payload.sha }}"
            
            if [ -d "$B_PATH/shared" ]; then
              cp -r $B_PATH/shared/* $B_PATH/releases/$SHA/
            fi
            
            ln -sfn $B_PATH/releases/$SHA $B_PATH/current
            cd $B_PATH/releases && ls -t | tail -n +6 | xargs -r rm -rf
            sudo systemctl restart ${{ secrets.BOT_SERVICE_NAME }}

      - name: Final Notification Bot
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_LOG_GROUP_ID }}" \
          -d "text=🎉 [Bot] Deployment Complete! Version: ${{ github.event.client_payload.sha }}"

  deploy-web:
    if: github.event.action == 'trigger_deploy_web'
    runs-on: ubuntu-latest
    steps:
      # 🚀 修正：直接在部署階段重新 Checkout 與編譯
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.client_payload.sha }}
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'
      - name: Build Web
        run: dotnet publish Mud9Bot.Web/Mud9Bot.Web.csproj -c Release -o ./publish_web

      - name: Deploy Web to Server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "publish_web/"
          target: "${{ secrets.WEB_DEPLOY_PATH }}/releases/${{ github.event.client_payload.sha }}"
          strip_components: 1
      - name: Swap Web & Restart
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_IP }}
          username: ${{ secrets.SSH_USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            W_PATH="${{ secrets.WEB_DEPLOY_PATH }}"
            SHA="${{ github.event.client_payload.sha }}"
            
            if [ -d "$W_PATH/shared" ]; then
              cp -r $W_PATH/shared/* $W_PATH/releases/$SHA/
            fi
            
            ln -sfn $W_PATH/releases/$SHA $W_PATH/current
            cd $W_PATH/releases && ls -t | tail -n +6 | xargs -r rm -rf
            sudo systemctl restart ${{ secrets.WEB_SERVICE_NAME }}

      - name: Final Notification Web
        run: |
          curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
          -d "chat_id=${{ secrets.TELEGRAM_LOG_GROUP_ID }}" \
          -d "text=🎉 [Web] Deployment Complete! Version: ${{ github.event.client_payload.sha }}"